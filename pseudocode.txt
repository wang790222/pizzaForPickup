
-----------HOME PAGE----------------
ButtonAddAPizza('/') {
  reveals pizza order box - allows client to specify pizza requirments
      -pizza order box has a delete or cancel button that deletes or removes that specific pizza
  each new click of the button creates a pizza order box

}
// describing what happens when the 'pizza' button is clicked


// ----We need to assign a cookie at this point --- cookie should track (1)pizza order (2) extra order (3) estimated time (4) cost
$('addNewPizza').on('click', function() {
  $('pizzaForm').slideToggle(400, function () {
  });
});
// describing what happens when the delete button inside the pizza 'box' is clicked
$('deletePizza').on('click', function() {
  $(this).closest('pizzaForm').remove()
})
// describes what happens when someone confirms that they have
// finished creating their pizza and want to add it to the order
$('addPizza').on('click', function() {
  createConfirmedPizza(cookie.body)
})

  function createConfirmedPizza(req) {
//cookie should be keeping an increasing tally of the cost!! something like let cost=req.session.cost
    $('pizzaForm').prepend(`
    <section class="pizza">
      <article>
          <p>Pizza</p><span>cost${cost}</span>
      </article>
    </section>`)
    $('<article>').addClass('pizza');
  };











ButtonORDER("/"){
  put order in orderdatabase
  retrieve the ordernumber
  redirect to "/:ordernuber"

}


------------customer submission page------------------
onbuttonSubmissionPage("/:ordernumber") {
  place name and telephone number into orderdatabase under orderid
  text restuarant
  gereates order on restuarant dashboard
  alters customer submission page to show status as 'pending'

}

------------resturant orders page----------------------
onbuttonConfirmOrder("/restuarant") {
  texts the customer with eta and confirmation
  updates the ETA in the database
  updates ETA on customer page and changes status to 'in progress'
  renders pickup button
}

onbuttonPickUp("/restuarant") {
  updates database with final order information
  removes order from display
  updates customer page to display that the order is complete
  -starts countdown to text message to ask for feedback
}